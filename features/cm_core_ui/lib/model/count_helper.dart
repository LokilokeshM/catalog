class CountHelper {
  int? level, rank, usersCount, ordersCount, productsCount;
  CountHelper(
      {this.level,
      this.rank,
      this.usersCount,
      this.ordersCount,
      this.productsCount});
}
